import type { Meta, StoryObj } from '@storybook/react'

import TripReportPage from './TripReportPage'

const meta: Meta<typeof TripReportPage> = {
  component: TripReportPage,
}

export default meta

type Story = StoryObj<typeof TripReportPage>

export const Primary: Story = {}
